// Template for local testing only — data/auth.js is what the site actually
// loads, and it's generated by the deploy workflow from the SITE_PASSWORD
// secret (never committed). To test locally:
//
//   1. Copy this file to data/auth.js
//   2. Replace the hash below with the SHA-256 hex digest of whatever
//      password you want to test with, e.g.:
//        printf '%s' 'yourpassword' | shasum -a 256
//   3. Log in locally with that password.
//
// data/auth.js is gitignored, so this never leaks a real password into git.

// hash below is sha256("changeme") — for local testing only, not the real password
window.SITE_PASSWORD_HASH = "057ba03d6c44104863dc7361fe4578965d1887360f90a0895882e58a6248fc86";
